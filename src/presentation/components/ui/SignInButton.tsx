'use client';

export function SignInButton() {
  return (
    <button
      type="button"
      className="btn btn-primary"
      aria-label="Sign in with Google"
    >
      Sign in with Google
    </button>
  );
}
